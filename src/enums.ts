
export enum Mode {
  Grid = 'grid',
  Vertical = 'vertical',
  Horizontal = 'horizontal'
}

export enum Data {
  Buffer = 'buffer',
  Path = 'path'
}

export enum Extension {
  JPG = 'jpg',
  JPEG = 'jpeg',
  PNG = 'png',
  BMP = 'bmp',
  GIF = 'gif',
  TIFF = 'tiff'
}
